apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-nodered
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: {{ .Values.nodered.subDomain }}.{{ .Values.global.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.nodered.fullnameOverride }}
                port:
                  number: 1880